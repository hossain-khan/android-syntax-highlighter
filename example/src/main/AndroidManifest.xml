<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".SampleApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
            All activities extend AppCompatActivity (which extends Activity).
            The Instantiatable lint check reports a false positive here because
            it expects the manifest entry to reference a direct subclass of
            android.app.Activity. Suppressing the warning is safe.
        -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.demohighlightjs.HighlightJsDemoActivity"
            android:parentActivityName=".ui.MainActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".ui.demoprismjs.PrismJsDemoActivity"
            android:parentActivityName=".ui.MainActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".ui.demoprismjs.PrismJsComposeDemoActivity"
            android:parentActivityName=".ui.MainActivity"
            tools:ignore="Instantiatable" />
    </application>

</manifest>